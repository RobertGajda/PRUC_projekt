transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vlog -vlog01compat -work work +incdir+X:/Studia/sem_m_1/PRUC/PONG_projekt {X:/Studia/sem_m_1/PRUC/PONG_projekt/Encoder.v}

